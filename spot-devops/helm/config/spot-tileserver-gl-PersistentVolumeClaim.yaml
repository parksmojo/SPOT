apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: spot-tileserver-gl
  namespace: spot
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: gp2
  volumeMode: Filesystem
